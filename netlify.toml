[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: client-side routes (/home, /about, ...) must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
