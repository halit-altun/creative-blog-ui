[build]
  # Netlify Base directory: frontend
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (API lives on Render — set VITE_API_URL in Netlify env)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
